linters:
    stylelint:
        config: 'stylelint.config.js'
        fixer: true
    csslint:
        ignore: 'no-descending-specificity,box-model,unique-headings,qualified-headings,duplicate-background-images,box-sizing,important,ids,known-properties,order-alphabetical,empty-rules'
files:
    ignore:
        - "src/generated/*"
        - "webroot/bower_components/*"
fixers:
   enable: true
